[package]
name = "wole"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "~4.5", features = ["derive"] }
easy-config-store = { version = "0.2.2", features = ["toml"] }
pnet = "0.35.0"
serde = { version = "1.0.228", features = ["derive"] }
